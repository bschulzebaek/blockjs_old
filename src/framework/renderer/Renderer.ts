import EventHelper from '../helper/EventHelper';
import { EVENT_STATE_RESUME } from '../state/state-events';

export default class Renderer {
    constructor() {
        // EventHelper.subscribe(EVENT_STATE_RESUME)
    }
}